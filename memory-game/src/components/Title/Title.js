import React from "react";
import "./Title.css";

const Title = props => (
	<div className="title">
		<h1>Baby Animal Clicky Game!</h1>
		<p>Earn points by clicking on images below, but only click each image once!</p>
	</div>
);

export default Title;